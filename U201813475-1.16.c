#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
/*void comp(int*p,int*q);
void main()
{
    printf("please input three numbers");
    int x,y,z;
    scanf("%d %d %d",&x,&y,&z);
    comp(&x,&y);
    comp(&y,&z);
    comp(&x,&y);
    printf("%d %d %d",x,y,z);
}
void comp(int*p,int*q)
{
    int i;
    if(*p<*q)
    {
        i=*p;
        *p=*q;
        *q=i;
    }
}*/

